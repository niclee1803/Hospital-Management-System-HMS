package usermenus;

/**
 * The {@code IUserMenu} interface represents the contract for user menus in the system.
 * It defines a method that each implementing class must provide to display the main menu
 * and handle user interactions for that menu.
 */
public interface IUserMenu {
    void mainMenu();
}
